# v1.0.5 — Public UI Cleanup

Removed from the public homepage:
- Large technical/statistics card (HTTPS / D1 / active ad count).
- Technical feature cards (Cloud images / Independent management / Analytics).

The hero was re-centered so the page remains balanced after removing those sections.

No database, API, admin, or migration changes.
