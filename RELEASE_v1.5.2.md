# v1.5.2 — Settings Visibility Fix

## Fixed
The `/admin/settings` page existed in v1.5.x, but the Admin dashboard did not actually render a navigation control to reach it.

## Added
- Visible `إعدادات الموقع` button in the Admin header.
- Highlighted Quick Settings card under Admin metrics.
- Direct navigation to `/admin/settings`.

No database or advertisement changes.
